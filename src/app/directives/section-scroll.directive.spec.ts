import { SectionScrollDirective } from './section-scroll.directive';

describe('SectionScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionScrollDirective();
    expect(directive).toBeTruthy();
  });
});
